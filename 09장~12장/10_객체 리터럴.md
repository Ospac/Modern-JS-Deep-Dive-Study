# 10장 객체 리터럴
## 10.1 객체란?
자바스크립트는 객체<sup>object</sup>기반 프로그래밍 언어이다. 원시 타입을 제외한 나머지 값(함수, 배열, 정규 표현식등)은 모두 객체이다.
- 원시 값은 변경 불가능한 값이지만, 객체는 변경 가능한 값이다. (프로퍼티 할당)
- 객체는 여러 프로퍼티로 구성되며, 프로퍼티는 키<sup>key</sup>와 값<sup>value</sup>로 구성된다.


## 10.2 객체 리터럴에 의한 객체 생성
생성자를 호출하여 객체를 생성하는 클래스 기반 객체지향 언어(C++, Java)와는 달리, 자바스크립트는 **프로토타입**(19장) 기반 객체지향 언어다. 따라서, 다양한 객체 생성 방법을 지원한다.
직관적인 방법은 **객체 리터럴**을 사용하는 것.
```js
var person = {
  name: 'Lee',
  sayHello: function () {
    console.log(`Hello! My name is ${this.name}.`);
  }
}; // 객체 리터럴의 중괄호는 코드 블록이 아니다. (코드블록은 끝에 세미콜론을 붙이지 않음)
```
## 10.3 프로퍼티
프로퍼티 키는 프로퍼티 값에 접근할 수 있는 식별자 역할을 한다.
- 식별자 네이밍 규칙에 따르지만, 따르지 않더라도 따옴표('')를 사용하면 키로 사용할 수 있다.
```js
var person = {
  firstName: 'Ung-mo', // 식별자 네이밍 규칙을 준수하는 프로퍼티 키
  'last-name': 'Lee'   // 식별자 네이밍 규칙을 준수하지 않는 프로퍼티 키
};
```
- 프로퍼티 키에 문자열이나 Symbol 값 외의 다른 값을 사용하면 암묵적 타입 변환을 통해 문자열이 된다.
- 이미 존재하는 프로퍼티 키를 중복 선언하면 새로운 키가 기존 키를 덮어쓴다.
## 10.4 메서드
자바스크립트의 함수는 일급객체(18.1)라서 값으로 취급 된다. 따라서, 프로퍼티 값으로 사용할 수 있다.
메서드<sup>method</sup>는 객체 내부의 함수를 의미한다.
```js
var circle = {
  radius: 5, // ← 프로퍼티
  getDiameter: function () { // ← 메서드
    return 2 * this.radius; // this는 circle을 가리킨다.
  }
};
```
## 10.5 프로퍼티 접근
표기법
- 마침표 표기법
  - `.` 사용
- 대괄호 표기법
  - `['...']` 사용 (따옴표를 사용하지 않으면 변수 식별자로 해석함)
  - 식별자 네이밍 규칙을 준수하지 않는 경우에는 대괄호 표기법을 사용해야함

객체에 존재하지 않는 프로퍼티에 접근하면 `undefined`를 반환한다. `ReferenceError`가 발생하지 않으므로 주의
## 10.6 프로퍼티 값 갱신
## 10.7 프로퍼티 동적 생성
존재하지 않는 프로퍼티에 값을 할당하면, 프로퍼티가 동적으로 생성되고 값이 할당된다.
## 10.8 프로퍼티 삭제
`delete` 연산자 활용
```js
var person = {
  name: 'Lee'
};
// 프로퍼티 동적 생성
person.age = 20;

delete person.age;
``` 
## 10.9 ES6에서 추가된 객체 리터럴의 확장 기능
### 10.9.1 프로퍼티 축약표현 (property shorthand)
프로퍼티 값으로 변수를 사용하는 경우, 변수 이름과 프로퍼티 키가 동일한 이름일 때 프로퍼티 키를 생략할 수 있음.

```js
let x = 1, y = 2;
// 프로퍼티 축약 표현
const obj = { x, y }; // {x:1, y:2}
```
### 10.9.2 계산된 프로퍼티 이름 (computed property name)
문자열로 변환될 수 있는 표현식을 사용해 프로퍼티 키를 동적을 생성할 수 있다.
```js
// ES5
var prefix = 'prop';

var obj = {};

// 계산된 프로퍼티 이름으로 프로퍼티 키 동적 생성
for(var i = 1; i <=> 3; i++>){
  obj[prefix + '-' + i] = i;
}
console.log(obj); // {prop-1: 1, prop-2: 2, prop-3: 3}
```

### 10.9.3 메서드 축약 표현
메서드를 정의할 때 function을 생략한다.
```js
const obj = {
  // 메서드 축약 표현
  sayHi() { // sayHi: function () { ... }
    ...
  }
};
```